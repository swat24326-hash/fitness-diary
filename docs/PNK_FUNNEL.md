# Воронка ПНК (потенциальный новый клиент)

**Статус:** в проде (2026-07) — линейный мастер 1|2 бесплатных, день визита пакетом.  
**Принцип:** та же карточка клиента; жизненный цикл `pnk` + абонемент БЗ. Оформление = платный ДК.  
**UX:** на каждом шаге только то действие, которое тренер должен сделать сейчас.

---

## Сценарий проверки (сквозной)

**Клиент:** «Сценарий ПНК Иванов», телефон `+79001234567`.

### 0. Подготовка
- В типах абонементов есть **БЗ** с флагом `is_pnk_trial` или кодом БЗ.
- Миграции ПНК применены (`npm run db:migrate:pnk -- --linked`).

### 1. Менеджер создаёт ПНК
1. `/sales` → плитка **ПНК** (или `/sales/pnk`).
2. Выбрать **1 или 2** бесплатные → тренер → передать.
3. Баннер Max / другой мессенджер — написать тренеру.

### 2. Тренер ведёт мастер (понятно по наитию)

| Шаг | Что на экране |
|-----|----------------|
| Контакт и дата | Max + дата → **Далее** в шапке |
| **Старт визита** | Изменить дату / Max → **Далее** (= клиент пришёл) |
| Здоровье | Карта ниже → **Далее** (пропуск нельзя) |
| Питание | Рацион → **Далее** или **Пропустить** |
| Тренировка | **Начать тренировку** → **Далее** (пропуск нельзя) |
| ДЗ | Вкладка ДЗ → **Далее** / **Пропустить** |
| (если 2) Тренировка 2 + ДЗ 2 | то же |
| Контакт после | Max → **Далее** / **Пропустить** |
| Оформление | Абонемент ДК → **Оформлен (ДК)** или отказ |

**Шапка воронки** (липкая): прогресс `N/M` + полоска + **Назад / Далее / Пропустить** на всех шагах до оформления. Тот же прогресс — на плитке доски менеджера и glance.

**Итог визита (качество):** блок «сделано / слабо / нет» — **только админ** (карточка клиента + панель на `/sales/pnk`). Менеджеру позже.

**Статистика:** KPI `оформления/ПНК` и маленький `%` — доска ПНК, статистика клуба и тренера. Карточка KPI кликабельна → разбор (отказы, открытые, питание/ДЗ, по тренерам).

Вкладки карточки — **только текущий подшаг**.

**Отказ без карточки:** при «Отказ» (и при удалении ПНК с доски) — событие в `pnk_funnel_events` (клуб/тренер/даты/флаги пакета, без имени и телефона) + полное удаление карточки и абонементов. Статистика = открытые/оформленные клиенты + журнал. Старые `pnk_lost` в базе ещё учитываются, пока их не удалят.

---

### 3. Цифры и роли
Как раньше: доска менеджера/админа, glance на главной тренера. Автопроверки: `verify-pnk-wizard`, `verify-pnk-stages`, … в `qa:local`.

## Deliverables

`contact` · `visit_started` · `health` · `nutrition` · `trial` · `homework` · (`trial2` · `homework2`) · `followup`

## Миграция

```bash
npm run db:migrate:pnk -- --linked
```
