# Менеджер по продажам + финансы клуба — ТЗ

**Статус:** ✅ **в проде** (роль `sales_manager`, маршруты `/sales`, `/sales/pnk`, `/sales/club-tasks`; админ — `/admin/sales`). Документ ниже — согласованное ТЗ и справочник полей; при расхождении с UI смотреть код (`AdminSales`, `api/_lib/adminData/salesHandlers.js`).  
**Связано:** воронка ПНК — [PNK_FUNNEL.md](./PNK_FUNNEL.md).  
**Не путать с:** [CLUB_SUPERVISOR.md](./CLUB_SUPERVISOR.md) — управляющий (ещё не роль в коде).

---

## Роли и зоны

| Роль | Клуб | Ежедневный отчёт | Расход (одна сумма) | План / сводки | Прочее |
|------|------|------------------|---------------------|---------------|--------|
| **Менеджер по продажам** | один свой | ✅ заполняет, правит прошлые дни | ❌ **не видит** | сосуд + свой клуб | отчёт + **прайс (редактирование своего клуба)** |
| **Управляющий** | один свой | 👁 просмотр отчётов | ✅ параметры + расход | свой клуб + **итоги** | **зона финансов** на той же странице |
| **Админ** | переключатель клуба | ✅ ввод + правка | ✅ параметры + расход | все клубы | **обе зоны** |

**Менеджер по продажам:** один на клуб.  
**Ежедневный отчёт:** один на **клуб / день**.

---

## Модуль 1 — Ежедневный отчёт отдела продаж

### Поля дня

| Поле | Код | Тип | Описание |
|------|-----|-----|----------|
| Дата | `report_date` | date | Календарный день |
| Клуб | `club_id` | uuid | UNIQUE вместе с датой |
| Прибыль **НК** | `profit_nk` | ₽ | Заработок за день по **новым** клиентам |
| Прибыль **ДК** | `profit_dk` | ₽ | По **действующим** |
| Прибыль **УК** | `profit_uk` | ₽ | По **ушедшим** |
| **Итого за день** | `profit_day` | ₽ | **Считает сайт:** `profit_nk + profit_dk + profit_uk` (не вводится отдельно) |
| ПНК | `pnk_total` | шт. | **Одно число** — потенциальные новые клиенты (**без** ПЗ/ТЗ/АЗ) |
| Тренировок проведено | `trainings_count` | шт. | **По всему клубу за день**, вводит **менеджер вручную** |

**Тренировки:** не тянем из планшетов тренеров — не у всех есть устройства. Отдельное поле в отчёте; позже можно **подсказку** из FIT-CITY (readonly), но **источник v1 — ручной ввод менеджера**.

### Когда что загружать

| Что | Файл / источник | Кто | Когда | Зачем |
|-----|-----------------|-----|-------|--------|
| **Отчёт по оплатам** | как `31.xlsx` из 1С | менеджер / админ | **Каждый день** (вечером или утром за вчера) | Подставить день в отчёт продаж (НК/ДК/УК). Ручной ввод по-прежнему можно |
| **Список заканчивающихся** | Excel 1С: карта + ФИО + дата окончания (+ тип карты ТЗ/АЗ, цена) | **только админ** | **Раз в период** → **Списки из Excel** (один файл) | Desk без тренера (`trainer_id` null) + `end_date` + `desk_hall` → вкладки ТЗ/АЗ |
| **Заявка тренеру (клип)** | руки из продажи / кассы → Ось | менеджер | **Днём**, сразу после продажи | Заявка «создай абон» (в т.ч. ДК/УК); правда = кнопка на планшете |
| **Входящее / вложение** | переписка | менеджер | **Вечером** | Только match карта→телефон; **без** create абона |
| **Книга часов ПЗ** | как `Kniga1.xlsx` | админ (карта ПЗ на Excel-списках) | позже | Занятия тренеров **без планшета** → матрица отчёта — UI-заглушка есть, парсер ещё нет |
| **Касса / DDS** | не грузим в Ось | — | — | Остаётся в 1С |

**Важно:** старые ручные дни отчёта сами **не пересчитываются**. Закрытия **не заменяют** дневной отчёт.

Служебный тренер «Не назначен» для desk **больше не нужен** (легаси-строки на holding по-прежнему вне KPI).

### Заявка тренеру на абон (день)

- UI: плитка / вкладка **«Заявка тренеру»** — `/sales?tab=clips` (менеджер) или `/admin/sales`.
- API: `admin-data?action=sale-clips` (GET список / POST create|cancel|match).
- Тренер после Sync видит awaiting на главной (пустое состояние: «заявок нет» + Sync) и в карточке клиента → **«Создать по заявке»** (`memberships.clip_id`, статус `done`).
- Мягкий чеклист дня: висящие awaiting — без блокировки отчёта.
- Verify: `node scripts/verify-sale-clips.mjs`.

### Импорт Excel «Отчёт по оплатам» (1С)

На вкладке дневного отчёта (админ и менеджер): загрузка `.xlsx` → превью строк (карта, зал, сумма) → галочки **НК / ДК / УК** → «Подставить в форму дня» → обычное **Сохранить**.

- Парсер: `src/lib/admin/salesPaymentsImportCore.js`
- ТЗ/АЗ без абона в Оси — сегмент **не** ставится автоматом (чтобы не было ложного НК)
- Клубная карта → «Доп. продажи»
- Касса / DDS / УК1 — не в этом контуре
- Verify: `node scripts/verify-sales-payments-import.mjs`

**Старые отчёты:** дни, уже заполненные вручную по прежней методике, **не пересчитываем** автоматически. Импорт — для новых дней (или явная ручная подстановка в выбранную дату). Сравнение месяца «до/после» старта импорта может дать ступеньку в цифрах — это ожидаемо, не баг кассы.

### Список заканчивающихся (админ → Списки из Excel)

Вкладка **`/admin/excel-lists`**: слева **«Список заканчивающихся (ТЗ и АЗ)»** (зал из файла), справа **«Часы персональных (ПЗ)»** — пока заглушка.

Один Excel из 1С: карта + ФИО + дата окончания (+ **цена**, **тип карты**). Зал берём из «Абонемент.Тип карты» (`ТЗ` / `ТЗ Утро` → `tz`, `АЗ` → `az`). Строки без ТЗ/АЗ не создаём. Клиенты → **`trainer_id = null`** + `desk_hall` + membership с `end_date` и `paid_amount`. Живые абоны не затираются. CHECK в БД: null-тренер только при `desk_hall` tz|az.

- UI: **Админ → Списки из Excel**. Результат: **Клиенты** → вкладки **ТЗ** / **АЗ**; с живым тренером — вкладка **Клиенты**.
- После сида: `desk_hall` = tz|az, без тренера; карточка **упрощённая** + блок **«Абонементы»**. KPI / «неактивные» desk не считают.
- Старые без зала: выбрать ТЗ/АЗ на карточке **или** снова загрузить Excel → действие «зал» (и `trainer_id` обнулится).
- Миграции: `npm run db:migrate:desk-hall -- --linked`, затем `npm run db:migrate:desk-null-trainer -- --linked`.
- Core: `deskClosingImportCore.js` / `deskClosingApplyService.js` / `holdingClientsCore.js` / `deskHallClientsCore.js`
- Verify: `verify-desk-closing-import.mjs`, `verify-desk-hall-clients.mjs`, `verify-sale-clips.mjs` (operational filter)

### Матрица 3×3 — только **количества** (целые ≥ 0)

**Без рублей**, без других единиц — только число в ячейке.

| | **НК** (новый) | **ДК** (действующий) | **УК** (ушедший) |
|---|:---:|:---:|:---:|
| **ПЗ** (персональный зал) | `pz_nk` | `pz_dk` | `pz_uk` |
| **ТЗ** (тренажёрный зал) | `tz_nk` | `tz_dk` | `tz_uk` |
| **АЗ** (аэробный зал) | `az_nk` | `az_dk` | `az_uk` |

### Правка прошлых дней

✅ **Админ** и **менеджер по продажам** (когда роль появится) — **без ограничения по давности** на v1 (при необходимости журнал изменений — позже).

### Связь со статистикой FIT-CITY

Отчёт продаж — **отдельный** ручной ввод. Не подменяет `memberships` / «Не активные».  
`trainings_count` **не равен** автоматически числу `trainings` в облаке, пока не у всех тренеров планшеты.

---

## Одна страница — две зоны (видимость по роли)

**URL:** `/admin/sales?club=<id>` — **один экран** для всех ролей.  
Состав блоков **зависит от роли**: менеджер видит **только зону A**; админ и управляющий — **A + B**.

### Зона A — «Отчёт продаж» (менеджер + админ + управляющий)

Менеджер **заполняет и сохраняет**. Управляющий — **только просмотр** (когда роль будет). Админ — ввод и правка (v1).

```
┌──────────────────────────────────────────────────────────┐
│  A1. ПЛАН МЕСЯЦА — «сосуд» (анимация)                   │
│      Факт Σ profit_day / plan_total                      │
│      План редактирует только админ (кнопка в зоне B)     │
├──────────────────────────────────────────────────────────┤
│  A2. Дата: [ … ]   ◀ ▶                                   │
├──────────────────────────────────────────────────────────┤
│  A3. Прибыль за день (₽):  НК  ДК  УК  → Итого (авто)    │
│  A4. ПНК (шт)                                            │
│  A5. Тренировок за день, клуб (шт) — вручную            │
│  A6. Матрица ПЗ/ТЗ/АЗ × НК/ДК/УК (шт)                    │
│  [ Сохранить отчёт ]  — только менеджер / админ          │
└──────────────────────────────────────────────────────────┘
```

### Зона B — «Финансы управляющего» (**менеджер не видит**)

Только **админ** (v1) и **управляющий** (позже). Блок **не рендерится** и **не отдаётся API** роли `sales_manager`.

```
┌──────────────────────────────────────────────────────────┐
│  B1. План на месяц (редактирование)                       │
│      plan_total, plan_pz, plan_tz, plan_az               │
├──────────────────────────────────────────────────────────┤
│  B2. Параметры управляющего за месяц                     │
│      expense_month (₽) — одна сумма расходов             │
│      (доп. поля — по мере согласования, v1: только расход)│
├──────────────────────────────────────────────────────────┤
│  B3. Итоги (считает сайт, только чтение)                 │
│      • Заработок месяца     = Σ profit_day               │
│      • Расход               = expense_month              │
│      • Чистая прибыль       = заработок − расход         │
│      • Σ profit_nk / dk / uk за месяц (опционально)      │
│      • Σ trainings_count за месяц (опционально)          │
│  [ Сохранить финансы / план ]                            │
└──────────────────────────────────────────────────────────┘
```

| Кто | Зона A | Зона B |
|-----|--------|--------|
| Менеджер по продажам | ✅ редактирует отчёт | ❌ **нет доступа** |
| Управляющий | 👁 просмотр | ✅ ввод расхода + **итоговые цифры** |
| Админ | ✅ всё | ✅ всё + выбор клуба |

**Важно:** это **одна страница**, не два URL. У менеджера нижняя часть (B) **отсутствует** — не «скрыта серым», а **не показывается**.

### Прайс персонального зала (менеджер)

Вкладка **Прайс** на `/sales?tab=price` — переключатель **ПЗ | ТЗ**. ПЗ: сетка карт, Excel, Save, печать/PNG. ТЗ: пакеты 1 мес + акции, Excel, Save (печать ТЗ — позже). Чужой клуб недоступен. Docs: [PRICE_LIST.md](./PRICE_LIST.md).

### Стратегия (менеджер и админ)

Вкладка **Стратегия** (`/sales?tab=strategy`):

1. **Якорь зала** — персональные часы и выручка из отчёта менеджера за прошлый месяц × календарь сезонности → ожидание на текущий/следующий месяц.
2. **Кусок ПЗ·ДК** — прайс × действующие карты × % продления (не весь план).

**Дата в шапке не меняется** при открытии вкладки (считает локально). Месяц плана в шапке переключается только после «В план». Матрицу и уровни сохраняет **управляющий/админ** во вкладке «План месяца». Docs: [PRICE_LIST.md](./PRICE_LIST.md) §P4. Verify: `scripts/verify-sales-hall-anchor.mjs`.

### «Сосуд» (анимация плана)

| | |
|---|---|
| Данные | `plan_total`; факт = **Σ `profit_day`** по сохранённым дням месяца |
| UI | Сосуд / банка, уровень = % плана; анимация при открытии и после сохранения дня |
| Кто меняет `plan_*` | **Админ** — блок B1; менеджер видит только сосуд в зоне A |

---

## Визуальный дизайн — **обязательное требование**

Экран продаж — **витрина для менеджера и руководства**, не «серая таблица в Excel». Должен выглядеть **красиво, современно, в духе FIT-CITY** (тёмная тема, зелёный акцент, мягкие карточки — как админ-панель и главная тренера).

### Общий язык

| Принцип | Реализация |
|---------|------------|
| Стиль | Тот же, что `admin-home`, `feature-tile`, `.card` — **не** чужой UI-kit |
| Фон | Тёмный градиент, лёгкое свечение акцента (`--accent-bright`, radial-gradient как у `admin-home__hero`) |
| Типографика | `section-title`, крупные **цифры итогов**, подписи `muted` |
| Отступы | `stagger` / `clamp()` — воздух между блоками, не плотная форма |
| Иконки | Lucide (как в приложении) — календарь, тренд, кошелёк |

### A1 — «Сосуд» с планом (главный визуальный акцент)

- **Центр экрана** в зоне A: стилизованный **сосуд / колба / банка** (SVG или CSS), не просто progress-bar.
- **Жидкость** — градиент зелёный → изумрудный, лёгкий **блик**; уровень = `факт / plan_total`.
- **Анимация:** плавное **заполнение** при открытии страницы (0.8–1.2 s, `ease-out`); после «Сохранить отчёт» — короткий **пульс / подъём уровня**, если изменился факт месяца.
- **Подпись:** «1 240 000 ₽ из 2 000 000 ₽» + **%** крупно; при ≥100% — мягкое «переполнение» (без кричащих цветов).
- **`prefers-reduced-motion`:** без анимации, только статичный уровень.

### A2–A6 — форма дня

- **Переключатель даты** — компактная «pill»-полоска (◀ дата ▶); тап по дате/календарю открывает нативный календарь (выбор любого дня, не только стрелки).
- **Прибыль НК / ДК / УК** — три **карточки-метрики** в ряд (на узком экране — колонка); у каждой подпись категории; **итого** — отдельная выделенная строка с авто-суммой.
- **ПНК и тренировки** — две небольшие карточки рядом (иконка + число).
- **Матрица 3×3** — аккуратная **таблица-сетка**: заголовки ПЗ/ТЗ/АЗ слева, НК/ДК/УК сверху; ячейки — округлые инпуты, hover/focus как у остальных форм.
- **Сохранить** — primary-кнопка; после сохранения — **toast** «Отчёт сохранён» (как в админке).

### Зона B — финансы (только admin / управляющий)

- **Визуально отделена**, но **в том же стиле**: тонкий разделитель или подзаголовок «Финансы клуба» (не другой сайт).
- **B3 Итоги** — **KPI-карточки** (заработок, расход, **чистая прибыль** крупнее остальных); чистая прибыль — главная цифра блока.
- План по направлениям — компактная **горизонтальная группа** полей или мини-таблица с подписями ПЗ / ТЗ / АЗ.
- **Ориентир ПЗ ДК:** кнопки «текущий / следующий месяц» сами открывают нужный месяц плана; % продления (по умолчанию 80%); для текущего вычитается факт шт. ПЗ ДК из дневных отчётов; превью → «В план». См. [PRICE_LIST.md](./PRICE_LIST.md) §P4.
- **План и прогноз** (`SalesFinanceForecast` + `clubFinanceForecastCore`):
  - **Текущий месяц:** четыре KPI — **План** → **Факт** → **Темп** → **Прогноз** (экстраполяция на конец месяца).
  - **Прошлый месяц:** только факт отчётов (`closed_month_fact`) — «Итоги месяца», без темпа/прогноза; колонка «Прогноз» скрыта.
  - **Будущий месяц:** ни факта, ни прогноза.
  - Отставание по залам — в таблице направлений. Когда блок факта уже в прогнозе/итогах, дублирующие KPI «Итоги месяца» не показываются.
  - Verify: `scripts/verify-club-finance-forecast.mjs`.
- **Сравнение плана по сегментам** (`SalesPlanMatrixCompareTable` + `salesPlanMatrixCompare`): статус «В темпе» / «Отстаём» — по **сумме к темпу** и **прогнозу** к концу месяца; отставание штук или среднего чека — чипы «штуки» / «чек». Verify: `scripts/verify-sales-plan-matrix-compare.mjs`.
- Без «сырых» `<table>` без стилей — только оформленные блоки.

### Адаптив

| Устройство | Ожидание |
|------------|----------|
| **ПК менеджера** (основной) | Широкий layout: сосуд по центру, форма в 2 колонки где уместно |
| Планшет | Одна колонка, сосуд чуть меньше |
| Телефон | Работает, но не primary — менеджер за компьютером |

### Техника UI (когда код)

- Страница: `src/pages/admin/AdminSales.jsx` (или аналог).
- Компоненты: `SalesPlanVessel.jsx`, `SalesDailyForm.jsx`, `SalesFinancePanel.jsx`.
- Стили: **`src/styles/sales-report.css`** (или блок в `index.css` с префиксом `.sales-report__`) — **не** раздувать JSX inline-стилями.
- Логика по-прежнему в `src/lib/admin/*`.

### Критерий «готово» по дизайну

Перед релизом фазы 1–2: экран **приятно открыть** — сосуд, цифры и карточки выглядят **цельно с остальным приложением**; менеджеру **хочется** пользоваться, а не «терпеть форму».

---

## Модуль 2 — План продаж (месяц)

**В первой версии** ✅

| Поле | Описание |
|------|----------|
| `plan_total` | **Общая сумма** плана на месяц (₽) |
| `plan_pz` | План по **ПЗ** (₽) |
| `plan_tz` | План по **ТЗ** (₽) |
| `plan_az` | План по **АЗ** (₽) |

**Факт за месяц (авто):**

- **Общий:** `Σ profit_day` по дням месяца ↔ `plan_total`.
- **По направлениям ПЗ/ТЗ/АЗ:** в дневном отчёте **нет** выручки по залам → **авто-факт по направлениям не считаем** (см. «Не реализуем»). План по направлениям **показываем**; факт по направлениям — вручную/следующая версия, если добавят поля.

---

## Модуль 3 — Расход управляющего и прибыль

| Решение | |
|---------|---|
| Ввод | **Одна сумма (₽)** за **календарный месяц** на клуб |
| Детализация | У управляющего **вне системы**; в FIT-CITY — только итог |
| Кто вносит в v1 | **Админ** (переключил клуб → финансы) |
| Кто вносит позже | **Управляющий** своего клуба |

### Итог по месяцам (сайт считает)

```
Итого заработок месяца  = Σ profit_day  (все дни месяца)
Расход управляющего     = expense_month (одна введённая сумма)
Чистая прибыль месяца   = Σ profit_day − expense_month
```

Сводки: по месяцам, по клубу; при необходимости — таблица дней + матрица (агрегаты количеств по НК/ДК/УК и ПЗ/ТЗ/АЗ).

**Тренировки в аэробном зале (статистика месяца):** клик по числу (тип или «Итого») открывает окно как у абонемента — список дней с количеством; тап по дню → дневной отчёт. Логика: `buildAerobicTypeDayBreakdown` в `aerobicSalesMatrix.js`.

**Новый тип АЗ (колонка Бокс / R1+ / R3+…):** админ создаёт в «Структура → Типы абон.» (раздел АЗ) и делает Sync. У менеджера — **«Обновить»** на странице отчёта (или Sync в шапке после деплоя фикса). Типы АЗ приходят из облака; без политики `fit_membership_types_sales_manager_read` и доступа к `action=membership-types` менеджер видел только устаревший локальный список.

**Админ:** параметр `?club=` — продажи, план/факт, финансы **выбранного клуба**.

---

## Модуль 4 — ИИ (DeepSeek)

**Не реализуем** до стабильных отчётов в БД. См. фазу 6.

---

## UI

```
/admin/sales?club=<id>   — единственная страница модуля «Продажи»
                           зона A (отчёт) + зона B (финансы, только admin/supervisor)
```

Сводка **за несколько месяцев** / архив — отдельный экран позже (не v1).

---

## Не реализуем (v1 / явно отложено)

| Что | Почему |
|-----|--------|
| Роль `sales_manager` в БД/UI | v1 — админ; роль — **фаза 4** |
| Роль `supervisor` (расход, просмотр) | **фаза 5** |
| ИИ / DeepSeek | **фаза 6** |
| ПНК по ПЗ / ТЗ / АЗ | ✅ решено: **только** `pnk_total` |
| Рубли / суммы в 9 ячейках матрицы | ✅ только **количества** |
| Авто-факт плана по ПЗ/ТЗ/АЗ | Нет дневной выручки по залам — только **общий** факт |
| Авто-подстановка `trainings_count` из FIT-CITY | v1 **ручной ввод**; подсказка из облака — позже |
| Менеджер: клиенты, абонементы, тренировки | ✅ **только отчёты** |
| Офлайн / sync отчётов на планшет | v1 **online-first** (рабочее место менеджера) |
| Лимит «сколько дней назад править» | Нет — правят админ и менеджер |

---

## Техника (когда будем делать)

1. **Таблицы:**
   - `club_sales_daily` — `club_id`, `report_date`, `profit_nk`, `profit_dk`, `profit_uk`, `pnk_total`, `trainings_count`, 9 int-колонок матрицы, `updated_at`, `updated_by`;
   - `club_sales_plan` — `club_id`, `year`, `month`, `plan_total`, `plan_pz`, `plan_tz`, `plan_az`;
   - `club_supervisor_expense` — `club_id`, `year`, `month`, `amount`.
2. **Логика:** `src/lib/admin/salesDailyReport.js`, `salesPlanAgg.js`, `clubProfitAgg.js` + `scripts/verify-club-sales-profit.mjs`.
3. **API:** `admin-data?action=sales-daily|sales-plan|sales-finance` — для `sales_manager` **не отдавать** finance/plan write и блок B.
4. **RLS + UI:** менеджер — только `club_sales_daily`; admin/supervisor — plan + expense + агрегаты.

---

## План фаз

| Фаза | Содержание | Статус |
|------|------------|--------|
| 0 | ТЗ (этот файл) | ✅ |
| 1 | SQL + страница: зона A (отчёт + **сосуд, визуал**) | ⏳ |
| 2 | Зона B на той же странице: план, расход, **KPI-итоги** (только admin) | ⏳ |
| 3 | Роли: sales_manager (только A), supervisor (A read + B) | отложено |

---

## Связанные документы

- [CLUB_SUPERVISOR.md](./CLUB_SUPERVISOR.md)
- [COMMERCIAL_ROADMAP.md](./COMMERCIAL_ROADMAP.md)

**Дата фиксации:** 2026-06-25 (одна страница, зоны A/B, **визуально красиво** в стиле FIT-CITY).
